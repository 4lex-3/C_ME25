//
// Created by alexm on 11.12.2025.
// Notendurchschnitt Anwendung

#include<stdio.h>
#include<stdlib.h>

int main() {
    char decision = '\0';
    int anz;
    printf("x.Klasse - Anzahl der Schüler:");
    scanf("%d", &anz);

    if (anz >0) {
        do {

        }while (decision == 'J' || decision == 'j');
    }
    printf("\t--E N D E--");

    return (EXIT_SUCCESS);
}